# ─── REQUIRED — the app will refuse to start without these ───────────────────

# Long random string — generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=replace-with-a-long-random-secret

# Comma-separated list of allowed browser origins
# Include your Cloudflare domain + the Flutter app scheme
CORS_ORIGINS=https://socratic.hx-ai.org,app://socratic-tutor

# Admin panel credentials
ADMIN_EMAIL=omarmnk5@gmail.com
ADMIN_PASSWORD=Oms@2002
JWT_SECRET_KEY=change-me-in-production
# ─── OPTIONAL — defaults are fine for most deployments ───────────────────────

# Path inside the container (matches the volume mount in docker-compose.yml)
MODEL_PATH=/app/models/socratic-q4_k_m.gguf

# 0 = CPU only; set to a positive integer if your server has a CUDA GPU
N_GPU_LAYERS=0

# Tune to your server's CPU core count
N_THREADS=4

N_CTX=4096
